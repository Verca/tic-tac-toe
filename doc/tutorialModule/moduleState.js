import Immutable from 'immutable';

export default Immutable.fromJS({
  // add your code here
  // items
  items: [
    { id: '007', name: 'one' },
    { id: '100', name: 'two' },
  ],
  loading: false,
});
